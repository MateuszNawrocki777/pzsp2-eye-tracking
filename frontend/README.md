# Frontend

Make sure your working directory is /frontend

Set the environment variables. The required variables are located in `.envexample`.
For development you can move the environment variables into `.env`:

```console
mv .envexample .env
```

To launch in dev mode run:
```console
npm run dev
```

Before launching for the first time run:
```console
npm install
```

